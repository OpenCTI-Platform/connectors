version: '3'
services:
  connector-cve:
    image: opencti/connector-cve:3.1.0
    environment:
      - OPENCTI_URL=${OPENCTI_URL}
      - OPENCTI_TOKEN=${OPENCTI_TOKEN}
      - CONNECTOR_ID=${CONNECTOR_ID}
      - CONNECTOR_TYPE=${CONNECTOR_TYPE}
      - CONNECTOR_NAME=${CONNECTOR_NAME}
      - CONNECTOR_SCOPE=${CONNECTOR_SCOPE}
      - CONNECTOR_CONFIDENCE_LEVEL=${CONNECTOR_CONFIDENCE_LEVEL}
      - CONNECTOR_UPDATE_EXISTING_DATA=${CONNECTOR_UPDATE_EXISTING_DATA}
      - CONNECTOR_LOG_LEVEL=${CONNECTOR_LOG_LEVEL}
      - CVE_IMPORT_HISTORY=${CVE_IMPORT_HISTORY}
      - CVE_NVD_DATA_FEED=${CVE_NVD_DATA_FEED}
      - CVE_HISTORY_DATA_FEED=${CVE_HISTORY_DATA_FEED}
      - CVE_INTERVAL=${CVE_INTERVAL} # Days
    restart: always
