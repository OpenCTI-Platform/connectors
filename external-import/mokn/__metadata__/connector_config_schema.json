{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://www.filigran.io/connectors/mokn_config.schema.json",
  "type": "object",
  "properties": {
    "OPENCTI_URL": {
      "description": "The OpenCTI platform URL.",
      "format": "uri",
      "maxLength": 2083,
      "minLength": 1,
      "type": "string"
    },
    "OPENCTI_TOKEN": {
      "description": "The token of the user who represents the connector in the OpenCTI platform.",
      "type": "string"
    },
    "CONNECTOR_ID": {
      "description": "A UUID v4 to identify the connector in OpenCTI.",
      "type": "string"
    },
    "CONNECTOR_NAME": {
      "default": "MokN",
      "description": "Name of the connector.",
      "type": "string"
    },
    "CONNECTOR_SCOPE": {
      "default": ["mokn"],
      "description": "The scope or type of data the connector is importing, either a MIME type or Stix Object (for information only).",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "CONNECTOR_TYPE": {
      "default": "EXTERNAL_IMPORT",
      "description": "Should always be set to EXTERNAL_IMPORT for this connector.",
      "type": "string"
    },
    "CONNECTOR_LOG_LEVEL": {
      "default": "info",
      "description": "Determines the verbosity of the logs.",
      "enum": ["debug", "info", "warn", "warning", "error"],
      "type": "string"
    },
    "CONNECTOR_DURATION_PERIOD": {
      "default": "PT1H",
      "description": "The period of time to await between two runs of the connector.",
      "type": "string"
    },
    "MOKN_CONSOLE_URL": {
      "description": "The base URL of the MokN console.",
      "format": "uri",
      "maxLength": 2083,
      "minLength": 1,
      "type": "string"
    },
    "MOKN_API_KEY": {
      "description": "Your MokN API key.",
      "type": "string"
    },
    "MOKN_TLP_LEVEL": {
      "default": "amber",
      "description": "TLP marking level for imported data.",
      "enum": ["clear", "white", "green", "amber", "amber+strict", "red"],
      "type": "string"
    },
    "MOKN_FIRST_RUN_DAYS_BACK": {
      "default": 30,
      "description": "Number of days to retrieve on first execution.",
      "exclusiveMinimum": 0,
      "type": "integer"
    }
  },
  "required": [
    "OPENCTI_URL",
    "OPENCTI_TOKEN",
    "CONNECTOR_ID",
    "CONNECTOR_NAME",
    "CONNECTOR_SCOPE",
    "CONNECTOR_TYPE",
    "MOKN_CONSOLE_URL",
    "MOKN_API_KEY"
  ],
  "additionalProperties": true
}
