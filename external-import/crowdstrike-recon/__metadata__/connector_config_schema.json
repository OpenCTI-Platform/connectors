{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://www.filigran.io/connectors/crowdstrike-recon_config.schema.json",
  "type": "object",
  "properties": {
    "OPENCTI_URL": {
      "description": "The base URL of the OpenCTI instance.",
      "format": "uri",
      "maxLength": 2083,
      "minLength": 1,
      "type": "string"
    },
    "OPENCTI_TOKEN": {
      "description": "The API token to connect to OpenCTI.",
      "type": "string"
    },
    "CONNECTOR_NAME": {
      "default": "CrowdStrike Recon",
      "description": "The name of the connector.",
      "type": "string"
    },
    "CONNECTOR_SCOPE": {
      "default": [
        "crowdstrike-recon"
      ],
      "description": "The scope of the connector.",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "CONNECTOR_LOG_LEVEL": {
      "default": "error",
      "description": "The minimum level of logs to display.",
      "enum": [
        "debug",
        "info",
        "warn",
        "warning",
        "error"
      ],
      "type": "string"
    },
    "CONNECTOR_TYPE": {
      "const": "EXTERNAL_IMPORT",
      "default": "EXTERNAL_IMPORT",
      "type": "string"
    },
    "CONNECTOR_DURATION_PERIOD": {
      "default": "PT1H",
      "description": "The period of time to await between two runs of the connector.",
      "format": "duration",
      "type": "string"
    },
    "CROWDSTRIKE_RECON_API_BASE_URL": {
      "description": "API base URL.",
      "type": "string"
    },
    "CROWDSTRIKE_RECON_CLIENT_ID": {
      "description": "CrowdStrike Falcon Client ID.",
      "type": "string"
    },
    "CROWDSTRIKE_RECON_CLIENT_SECRET": {
      "description": "CrowdStrike Falcon Client Secret.",
      "type": "string"
    },
    "CROWDSTRIKE_RECON_TLP_LEVEL": {
      "default": "amber+strict",
      "description": "Default TLP level of the imported entities.",
      "enum": [
        "clear",
        "white",
        "green",
        "amber",
        "amber+strict",
        "red"
      ],
      "type": "string"
    },
    "CROWDSTRIKE_RECON_IMPORT_START_DATE": {
      "default": "P10D",
      "description": "ISO 8601 duration string specifying how far back to import alerts (e.g., P1D for 1 day, P7D for 7 days)",
      "format": "duration",
      "type": "string"
    },
    "CROWDSTRIKE_RECON_FILTER_TOPIC": {
      "default": "",
      "description": "Filter notifications by topic name(s). Comma-separated string (e.g. 'SA_BRAND,SA_THIRD_PARTY_V2'). Empty means no filtering.",
      "type": "string"
    },
    "CROWDSTRIKE_RECON_FILTER_TYPE": {
      "default": "",
      "description": "Filter notifications by item type(s). Comma-separated string (e.g. 'typosquatting_domain,exposed_data'). Empty means no filtering.",
      "type": "string"
    },
    "CROWDSTRIKE_RECON_FILTER_PRIORITY": {
      "default": "",
      "description": "Filter notifications by priority(ies). Comma-separated string (e.g. 'high,medium,low'). Empty means no filtering.",
      "type": "string"
    }
  },
  "required": [
    "OPENCTI_URL",
    "OPENCTI_TOKEN",
    "CROWDSTRIKE_RECON_API_BASE_URL",
    "CROWDSTRIKE_RECON_CLIENT_ID",
    "CROWDSTRIKE_RECON_CLIENT_SECRET"
  ],
  "additionalProperties": true
}