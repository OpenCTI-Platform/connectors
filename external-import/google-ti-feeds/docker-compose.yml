services:
  connector-google-ti-feeds:
    image: opencti/connector-google-ti-feeds:6.6.11
    environment:
      - OPENCTI_URL=
      - OPENCTI_TOKEN=
      - CONNECTOR_ID=
      - GTI_API_KEY=
    restart: unless-stopped
    networks:
      - docker_default

networks:
  default:
    external: true
    name: docker_default
