version: '3'
services:
  connector-sekoia:
    image: opencti/connector-sekoia:6.9.0
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=ChangeMe
      - SEKOIA_API_KEY=ChangeMe
    restart: always