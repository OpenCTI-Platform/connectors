version: "3"
services:
  connector-threatfox:
    image: opencti/connector-threatfox:rolling
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=ChangeMe
    restart: always
