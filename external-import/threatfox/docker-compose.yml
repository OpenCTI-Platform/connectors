version: "3"
services:
  connector-threatfox:
    image: opencti/connector-threatfox:6.8.15
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=ChangeMe
    restart: always
