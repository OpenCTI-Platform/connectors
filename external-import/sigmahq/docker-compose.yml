version: '3'
services:
  connector-sigmahq:
    image: opencti/connector-sigmahq:latest
    environment:
      - OPENCTI_URL=http://localhost
      - OPENCTI_TOKEN=ChangeMe
      # - CONNECTOR_ID=ChangeMe
      # - CONNECTOR_NAME=SigmaHQ
      # - CONNECTOR_SCOPE=Indicator,Attack-Pattern,Vulnerability,Identity,Marking-Definition,Relationship
      # - CONNECTOR_LOG_LEVEL=error
      # - CONNECTOR_DURATION_PERIOD=P1D
      # - SIGMAHQ_RULE_PACKAGE=sigma_all_rules
      # - SIGMAHQ_TLP_LEVEL=clear
    restart: always
