services:
  connector-mitre:
    image: opencti/connector-mitre:6.8.9
    environment:
      - OPENCTI_URL=http://localhost
      - OPENCTI_TOKEN=ChangeMe
#      - MITRE_REMOVE_STATEMENT_MARKING=true
#      - MITRE_INTERVAL=7 # In days
#    restart: always
