{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://www.filigran.io/connectors/ft3_config.schema.json",
  "type": "object",
  "properties": {
    "OPENCTI_URL": {
      "description": "The OpenCTI platform URL.",
      "format": "uri",
      "maxLength": 2083,
      "minLength": 1,
      "type": "string"
    },
    "OPENCTI_TOKEN": {
      "description": "The token of the user who represents the connector in the OpenCTI platform.",
      "type": "string"
    },
    "CONNECTOR_NAME": {
      "default": "FT3 Framework",
      "description": "Name of the connector.",
      "type": "string"
    },
    "CONNECTOR_SCOPE": {
      "default": [
        "identity",
        "attack-pattern",
        "x-mitre-tactic",
        "x-opencti-kill-chain",
        "marking-definition"
      ],
      "description": "The scope or type of data the connector is importing, either a MIME type or Stix Object (for information only).",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "CONNECTOR_TYPE": {
      "default": "EXTERNAL_IMPORT",
      "description": "Should always be set to EXTERNAL_IMPORT for this connector.",
      "type": "string"
    },
    "CONNECTOR_LOG_LEVEL": {
      "default": "error",
      "description": "Determines the verbosity of the logs.",
      "enum": [
        "debug",
        "info",
        "warn",
        "warning",
        "error"
      ],
      "type": "string"
    },
    "FT3_INTERVAL": {
      "default": 5,
      "description": "Polling interval in days for fetching and refreshing FT3 data. Determines how often the system checks for updates to FT3 datasets.",
      "exclusiveMinimum": 0,
      "type": "integer"
    },
    "FT3_TACTICS_URL": {
      "default": "https://raw.githubusercontent.com/stripe/ft3/refs/heads/master/FT3_Tactics.json",
      "description": "URL to the FT3 Tactics JSON file. This dataset includes fraud tactics from the FT3 framework.",
      "type": "string"
    },
    "FT3_TECHNIQUES_URL": {
      "default": "https://raw.githubusercontent.com/stripe/ft3/refs/heads/master/FT3_Techniques.json",
      "description": "URL to the FT3 Techniques JSON file. Contains fraud techniques and their relationships to tactics.",
      "type": "string"
    }
  },
  "required": [
    "OPENCTI_URL",
    "OPENCTI_TOKEN"
  ],
  "additionalProperties": true
}