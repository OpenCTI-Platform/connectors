version: '3'
services:
  connector-proofpoint-tap:
    image: opencti/connector-proofpoint-tap:latest
    env_file:
      - .env
    restart: always

    # networks:
    #   - docker_default

# networks:
#   default:
#     external: true
#     name: docker_default
