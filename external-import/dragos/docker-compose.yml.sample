services:
  connector-proofpoint-tap:
    # build: .
    image: opencti/connector-dragos:6.6.6
    env_file:
      - .env
    restart: unless-stopped

    # networks:
    #   - docker_default

# networks:
#   default:
#     external: true
#     name: docker_default