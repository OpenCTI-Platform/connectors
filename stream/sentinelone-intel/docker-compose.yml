version: '3'
services:
  connector-template:
    image: opencti/connector-template:6.7.11
    environment:
      # Connector's generic execution parameters
      - OPENCTI_URL=http://localhost
      - OPENCTI_TOKEN=CHANGEME
      # Connector's definition parameters REQUIRED
      - CONNECTOR_ID=CHANGEME
      - CONNECTOR_NAME=CHANGEME
      - CONNECTOR_SCOPE=CHANGEME
      - CONNECTOR_LOG_LEVEL=error
      - CONNECTOR_LIVE_STREAM_ID=live # ID of the live stream created in the OpenCTI UI
      - CONNECTOR_LIVE_STREAM_LISTEN_DELETE=true
      - CONNECTOR_LIVE_STREAM_NO_DEPENDENCIES=true

      # Connector's custom execution parameters
      - SENTINELONE-INTEL_API_URL=CHANGEME
      - SENTINELONE-INTEL_API_KEY=CHANGEME
      - SENTINELONE-INTEL_ACCOUNT_ID=CHANGEME
      - SENTINELONE-INTEL_SITE_ID=CHANGEME
      - SENTINELONE-INTEL_GROUP_ID=CHANGEME


      # Add proxy parameters below if needed
      # - HTTP_PROXY=CHANGEME
      # - HTTPS_PROXY=CHANGEME
      # - NO_PROXY=CHANGEME
    restart: always
    # networks:
    #   - docker_default

# networks:
#   default:
#     external: true
#     name: docker_default
