version: '3'
services:
  connector-google-dns:
    image: opencti/connector-google-dns:rolling
    environment:
      - OPENCTI_URL=http://localhost
      - OPENCTI_TOKEN=ChangeMe
    restart: always
