version: '3'
services:
  connector-virustotal-downloader:
    image: opencti/connector-virustotal-downloader:6.8.4
    environment:
      - OPENCTI_URL=ChangeMe
      - OPENCTI_TOKEN=ChangeMe
      - VIRUSTOTAL_DOWNLOADER_API_KEY=ChangeMe
    restart: always
