opencti:
  url: 'http://localhost'
  token: 'ChangeMe'

connector:
  type: 'INTERNAL_ENRICHMENT'
  id: 'ChangeMe'
  name: 'IPQS Fraud and Risk Scoring'
  # ``User-Account`` activates the Darkweb-Leak branch (PR #6399);
  # ``Artifact`` activates the malware-file-scanner branch
  # (PR #5970 / issue #6199).
  scope: 'IPv4-Addr,Email-Addr,Phone-Number,Domain-Name,Url,User-Account,Artifact'
  auto: true # Enable/disable auto-enrichment of observables
  confidence_level: 15 # From 0 (Unknown) to 100 (Fully trusted)
  log_level: 'info'

ipqs:
  private_key: 'ChangeMe'
  base_url: 'https://ipqualityscore.com/api/json'

  # IP specific config settings
  ip_add_relationships: true # Whether or not to add ASN relationships

  # Domain specific config settings
  domain_add_relationships: true # Whether or not to add IPv4, Domain relationships

  # TLP handling. ``max_tlp`` is enforced before the entity-type dispatch
  # and therefore applies to **every** enrichment branch (IP / Email /
  # URL / Phone / User-Account / Artifact). ``default_tlp`` is the
  # marking attached to the STIX objects emitted by the Artifact /
  # failure-note branches when the observable carries no explicit marking.
  default_tlp: 'TLP:CLEAR' # Used when the observable carries no explicit marking.
  max_tlp: 'TLP:AMBER' # Observables with a higher TLP are skipped, on every branch.