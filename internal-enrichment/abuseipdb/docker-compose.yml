version: '3'
services:
  connector-abuseipdb:
    image: opencti/connector-abuseipdb:6.8.4
    environment:
      - OPENCTI_URL=http://localhost
      - OPENCTI_TOKEN=ChangeMe
      - ABUSEIPDB_API_KEY=ChangeMe
    restart: always
